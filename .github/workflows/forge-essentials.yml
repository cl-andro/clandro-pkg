name: Forge Essentials (aarch64)

on:
  workflow_dispatch:

jobs:
  build-essentials:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/termux/package-builder:latest
      options: --privileged
    steps:
      - name: Checkout Sovereign Code
        uses: actions/checkout@v4

      - name: Architect the Custom OS Directories
        run: |
          sudo mkdir -p /data/data/com.zk.clandro/files/usr
          sudo mkdir -p /data/data/com.zk.clandro/files/home
          sudo chown -R builder:builder /data/data/com.zk.clandro

      - name: Install the C++ Compiler (The Missing NDK)
        run: |
          export HOME=/home/builder
          ./scripts/setup-android-sdk.sh

      - name: Forge Essentials (Surgical Execution)
        run: |
          export HOME=/home/builder
          
          # 1. Base Bedrock & Libraries
          ./build-package.sh -I -a aarch64 ndk-sysroot
          ./build-package.sh -I -a aarch64 libandroid-support
          ./build-package.sh -I -a aarch64 libandroid-glob
          ./build-package.sh -I -a aarch64 libbz2
          ./build-package.sh -I -a aarch64 liblzma
          ./build-package.sh -I -a aarch64 libmd
          ./build-package.sh -I -a aarch64 libandroid-posix-semaphore
          ./build-package.sh -I -a aarch64 libgmp
          ./build-package.sh -I -a aarch64 libmpfr
          ./build-package.sh -I -a aarch64 pcre2
          ./build-package.sh -I -a aarch64 libandroid-selinux
          ./build-package.sh -I -a aarch64 libcap-ng
          
          # 2. Terminal UI Bedrock
          ./build-package.sh -I -a aarch64 libiconv
          ./build-package.sh -I -a aarch64 ncurses
          ./build-package.sh -I -a aarch64 readline
          
          # 3. Crypto & Network Stack
          ./build-package.sh -I -a aarch64 ca-certificates
          ./build-package.sh -I -a aarch64 zlib
          ./build-package.sh -I -a aarch64 openssl
          ./build-package.sh -I -a aarch64 libssh2
          ./build-package.sh -I -a aarch64 libnghttp2
          ./build-package.sh -I -a aarch64 libnghttp3
          ./build-package.sh -I -a aarch64 brotli
          ./build-package.sh -I -a aarch64 libngtcp2
          ./build-package.sh -I -a aarch64 libcurl
          
          # 4. Termux Core Bedrock
          ./build-package.sh -I -a aarch64 termux-core
          ./build-package.sh -I -a aarch64 termux-exec
          ./build-package.sh -I -a aarch64 termux-am
          ./build-package.sh -I -a aarch64 termux-am-socket
          
          # 5. Core POSIX Utilities
          ./build-package.sh -I -a aarch64 dash
          ./build-package.sh -I -a aarch64 bash
          ./build-package.sh -I -a aarch64 coreutils
          ./build-package.sh -I -a aarch64 tar
          ./build-package.sh -I -a aarch64 gzip
          ./build-package.sh -I -a aarch64 grep
          ./build-package.sh -I -a aarch64 sed
          ./build-package.sh -I -a aarch64 gawk
          ./build-package.sh -I -a aarch64 findutils
          ./build-package.sh -I -a aarch64 diffutils
          ./build-package.sh -I -a aarch64 less
          ./build-package.sh -I -a aarch64 procps
          ./build-package.sh -I -a aarch64 psmisc
          ./build-package.sh -I -a aarch64 util-linux
          
          # 6. Dialog, Termux Tools, Nano, Keyring & Diffutils
          ./build-package.sh -f -I -a aarch64 dialog
          ./build-package.sh -f -I -a aarch64 termux-tools
          ./build-package.sh -I -a aarch64 nano
          ./build-package.sh -I -a aarch64 termux-keyring
          ./build-package.sh -f -I -a aarch64 diffutils

      - name: Sanitize Artifact Filenames
        if: success()
        run: |
          # Find any .deb file with a colon and replace it using standard 'tr'
          for f in output/*:*.deb; do
            if [ -e "$f" ]; then
              new_name=$(echo "$f" | tr ':' '_')
              mv "$f" "$new_name"
            fi
          done

      - name: The Autopsy (Catch the Crash)
        if: failure()
        run: |
          echo "========== CONFIG.LOG AUTOPSY START =========="
          find /home/builder/.termux-build/ -name "config.log" -exec cat {} +
          echo "========== CONFIG.LOG AUTOPSY END =========="

      - name: Upload Essentials Artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: cl-andro-essentials
          path: output/*.deb

      - name: Publish & Sign Packages
        if: success()
        env:
          CL_ANDRO_PAT: ${{ secrets.CL_ANDRO_PAT }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
        run: |
          ./scripts/publish-repo-ci.sh
